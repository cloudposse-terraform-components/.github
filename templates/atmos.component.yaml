base_path: "./"

components:
  terraform:
    base_path: "/src"
    apply_auto_approve: false
    deploy_run_init: true
    init_run_reconfigure: true
    auto_generate_backend_file: false

stacks:
  base_path: "test/fixtures/stacks"
  included_paths:
    - "orgs/**/*"
  excluded_paths:
    - "**/_defaults.yaml"
  name_pattern: "{stage}"

logs:
  file: "/dev/stderr"
  level: Info

docs:
  generate:
    # Generate README for the root directory
    readme:
      base-dir: .
      input:
        - "./README.yaml"
      # TODO point to main
      # template: "https://raw.githubusercontent.com/cloudposse-terraform-components/.github/blob/main/templates/README.md.gotmpl"
      template: "https://raw.githubusercontent.com/cloudposse-terraform-components/.github/e872f070f473a43c2540b6b1c604b085b7445759/templates/README.md.gotmpl"
      output: "README.md"
      terraform:
        source: src/
        enabled: false
        format: "markdown"
        show_providers: false
        show_inputs: true
        show_outputs: true
        sort_by: "name"
        hide_empty: false
        indent_level: 2

    # Generate README for the defined component
    component:
      base-dir: src/
      input:
        - "./README.yaml"
      # TODO point to main
      # template: "https://raw.githubusercontent.com/cloudposse-terraform-components/.github/blob/main/templates/README.md.simple.gotmpl"
      template: "https://raw.githubusercontent.com/cloudposse-terraform-components/.github/e872f070f473a43c2540b6b1c604b085b7445759/templates/README.md.simple.gotmpl"
      output: "README.md"
      terraform:
        source: src/
        enabled: false
        format: "markdown"
        show_providers: false
        show_inputs: true
        show_outputs: true
        sort_by: "name"
        hide_empty: false
        indent_level: 2
